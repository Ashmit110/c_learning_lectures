#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    double a=3;double b=4 ;double c=5;
    if ((pow(a,2)+pow(b,2))==pow(c,2))
        {
            printf("present");
    return 0;
}
}
